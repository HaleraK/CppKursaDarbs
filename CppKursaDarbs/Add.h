#pragma once
#include <string>
#include <iostream>
#include <vector>
#include <sstream> 

using std::string;
using std::vector;

void addUser();
void addDrawing();
void add();
void createUser();
void createDrawing();
string createOneWord(string input, int pos);
string createLastParam(string input, int pos);